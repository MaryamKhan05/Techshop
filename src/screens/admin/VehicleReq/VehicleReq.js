import React from "react";
import { View, Text, StyleSheet } from "react-native";

const VehicleReq = () => {
  return (
    <View>
      <Text>VehicleReq Screen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default VehicleReq;
